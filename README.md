# Kilangi — Frontend Assignment

This is a responsive eCommerce homepage built with HTML, CSS, and JavaScript to match the provided design mockups (desktop and mobile).

How to run locally

1. Open `index.html` in your browser (no build step required).

Deployment

- You can host this repository with GitHub Pages, Netlify, or Vercel by connecting the repo and deploying the `index.html` directly.

Notes

- Fonts: Google Fonts (Playfair Display and Inter) are used to match the design.
- Images are from Unsplash placeholder links and can be replaced with final assets.
